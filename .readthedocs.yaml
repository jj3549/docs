version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"
  jobs:
    build:
      html:
        - sphinx-build -W . $READTHEDOCS_OUTPUT/html

python:
  install:
    - requirements: requirements.txt
